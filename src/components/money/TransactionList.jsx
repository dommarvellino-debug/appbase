import { Trash2, ShoppingBag, Home, Bus, BookOpen, Smile, HeartPulse, CircleDollarSign, Shapes } from 'lucide-react';
import { useCurrency } from '@/contexts/CurrencyContext';
const icons={Food:ShoppingBag,Housing:Home,Transport:Bus,Study:BookOpen,Fun:Smile,Health:HeartPulse,Income:CircleDollarSign,Other:Shapes};
export default function TransactionList({ transactions, onDelete }) {
  const { formatMoney: money } = useCurrency();
  return <section className="rounded-3xl border border-slate-200/70 bg-white p-5 shadow-sm"><div><h2 className="font-bold">Recent activity</h2><p className="text-sm text-slate-500">Your latest transactions</p></div><div className="mt-4 divide-y divide-slate-100">{transactions.length ? transactions.slice(0,6).map(t=>{const Icon=icons[t.category]||Shapes;return <div key={t.id} className="group flex items-center gap-3 py-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-slate-100 text-slate-600"><Icon size={18}/></span><div className="min-w-0"><p className="truncate text-sm font-semibold">{t.title}</p><p className="text-xs text-slate-400">{t.category} · {new Date(`${t.date}T00:00:00`).toLocaleDateString('en-US',{month:'short',day:'numeric'})}</p></div><p className={`ml-auto text-sm font-bold ${t.type==='income'?'text-emerald-600':'text-slate-900'}`}>{t.type==='income'?'+':'−'}{money(t.amount)}</p><button onClick={()=>onDelete(t.id)} aria-label="Delete transaction" className="p-1 text-slate-300 transition hover:text-rose-500 sm:opacity-0 sm:group-hover:opacity-100"><Trash2 size={16}/></button></div>}) : <p className="py-12 text-center text-sm text-slate-400">Your transactions will appear here.</p>}</div></section>;
}